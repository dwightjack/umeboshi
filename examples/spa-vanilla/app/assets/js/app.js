/**
 * Main Application File
 *
 * Use for bootstrapping large application
 * or just fill it with JS on small projects
 *
 */

const box = document.createElement('p');

box.className = 'c-box';
box.innerHTML = 'Hello Worldxxxx';

document.body.appendChild(box);
